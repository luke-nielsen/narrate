"""Built-in renderer plugins shipped with Narrate.

Every module here registers through the ``narrate.renderers`` entry-point
group declared in ``pyproject.toml`` -- exactly the same mechanism
third-party renderers use, so the core has no special knowledge of them.
"""
